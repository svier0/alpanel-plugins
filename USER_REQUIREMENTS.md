# 用户原始需求记录（逐字）

> 用户要求：所有 UI 改动必须逐字对齐这里记录的原文，禁止自行发挥、加字、删字。

## mysql 插件 - 二进制日志页（2026-08-01）

用户原话（逐字）：

1. `二进制日志 开关控件 2.51 KB 看不懂吗？`
   - 页面必须有标题「二进制日志」
   - 必须是一个 switch 开关控件
   - 后面跟大小数字（格式如 `2.51 KB`）

2. `二进制日志 <switch> 2.51kb 你他妈是不是看不懂 你的"二进制日志"在哪里 你的"未开启" "总大小："是哪来的`
   - 标题「二进制日志」必须显示
   - 不要「未开启」文字
   - 不要「总大小：」前缀，直接显示大小数字

3. `操你妈逼谁让你加粗的 谁让你换行的`
   - 标题不加粗
   - 标题不换行（与 switch、大小同行）

4. `还有温馨提示语老子发给你的是什么 让你逐字对齐听不懂吗`
   - 温馨提示原文（用户提供的逐字文本）：
   - `· 温馨提示：MySQL二进制日志可用于数据备份恢复。`

当前实现状态：标题已改为 span 不加粗不换行；switch 纯滑块无文字；大小直接显示。

## 待确认 / 待补充

- [ ] 温馨提示原文（用户还没重新发）
- [ ] 「操作」列删除按钮是否符合
- [ ] 表格列：文件名 / 大小 / 最后修改时间 / 操作 是否对
